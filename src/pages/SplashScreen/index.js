import React from 'react';
import { ScrollView, Text } from 'react-native';

const SplashScreen = () => {
  return (
    <ScrollView showsVerticalScrollIndicator={false}>
      <Text>SlapshScreen will be here!</Text>
    </ScrollView>
  );
};

export default SplashScreen;
